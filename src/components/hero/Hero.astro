  <section class="hero" aria-labelledby="hero-title">
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <h1 id="hero-title" class="hero-title">Bufandas para tu compañero peludo.</h1>
      
      <p class="hero-subtitle">
        Diseñamos piezas únicas en lana natural, tejidas a mano y acabadas con detalles
        que conservan el espíritu. Cada prenda se construye bajo pedido para
        abrazar a tu compañero peludo con suavidad y estilo.
      </p>

      <div class="hero-atelier-card">
        <div>
          <h4>Hecho en un taller familiar</h4>
        </div>
      </div>
      <div class="hero-cta">
        <a class="btn" href="#products">Ver productos</a>
        <a class="btn btn-secondary" href="#collections">Ver colecciones</a>
      </div>
      <div class="hero-stats">
        <div class="hero-stat">
          <span class="hero-stat-number">+10</span>
          <span class="hero-stat-label">Mascotas abrigadas</span>
        </div>
        <div class="hero-stat">
          <span class="hero-stat-number">72h</span>
          <span class="hero-stat-label">Envuelto y enviado</span>
        </div>
        <div class="hero-stat">
          <span class="hero-stat-number">100%</span>
          <span class="hero-stat-label">Lana natural</span>
        </div>
      </div>
    </div>
  </section>


  <style>
    /* Hero Section */
    .hero {
    position: relative;
    height: 100vh;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-image: url('/images/hero/1.webp');
    background-size: cover;
    background-position: 50% 10%;
    background-repeat: no-repeat;
    }

    .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.35);
    z-index: 1;
    }
    .hero-overlay {
      width: 100%;
      position: absolute;
      top: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.25);
    }

    /* Hero Content */
    .hero-content {
    position: relative;
    z-index: 3;
    text-align: center;
    color: white;
    max-width: 900px;
    padding: 2rem;
    animation: fadeInUp 1s ease-out;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    }

    @keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
    }

    .hero-title {
    font-family: var(--font-display);
    font-size: 5rem;
    line-height: 1;
    color: white;
    font-weight: 400;
    margin-bottom: 1.5rem;
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.4);
    }

    .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.3rem);
    color: rgba(255, 255, 255, 0.95);
    max-width: 700px;
    margin: 0 auto 2rem;
    text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.3);
    font-weight: 300;
    }

    /* Hero Atelier Card */
    .hero-atelier-card {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 1.5rem;
    border-radius: 50px;
    margin-bottom: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
    }

    .hero-atelier-card i {
    font-size: 1.5rem;
    color: var(--color-primary-light);
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
    }

    .hero-atelier-card h4 {
    margin: 0;
    color: white;
    font-size: 1rem;
    font-weight: 500;
    text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.3);
    }

    /* Hero CTA Button */
    .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
    justify-content: center;
    margin-bottom: 3rem;
    }

    .hero-cta .btn {
    padding: 1rem 3rem;
    font-size: 1.1rem;
    border-radius: 50px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    transition: all 0.5s ease;
    background: var(--color-primary);
    border: 1px solid var(--color-primary);
    cursor: pointer;
    }

    .hero-cta .btn:hover {
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
    background: transparent;
    border: 1px solid var(--color-primary);
    }

    /* Hero Stats */
    .hero-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    flex-wrap: wrap;
    }

    .hero-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 1rem;
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
    min-width: 185px;
    }

    .hero-stat-number {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    font-weight: 600;
    color: var(--color-primary-light);
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.4);
    }

    .hero-stat-label {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.9);
    text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.3);
    text-align: center;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    }

  @media (max-width: 768px) {
    .hero{
      height: 100% !important;
      overflow: auto !important;
    }
    .hero-content{
      padding: 10rem 0;
      height: 100%;
    }
  }
  </style>